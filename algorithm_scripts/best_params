the following parameters are the ones that gave the best results when using a grid search.
if they are blank, then they haven't been found yet.

Dense Neural network
  network size:
  epochs:
  optimizer:
  

Convolutional Neural network

LSTM neural network
